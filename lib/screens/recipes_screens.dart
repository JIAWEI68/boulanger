import 'package:flutter/material.dart';

class RecipesScreens extends StatelessWidget {
  const RecipesScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
